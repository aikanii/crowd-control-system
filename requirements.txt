# Core dependencies - modern, compatible versions
# Use opencv-python-headless for servers/docker without GUI
# For desktop with GUI, use opencv-python instead
opencv-python-headless>=4.5.0
numpy>=1.21.0,<2
scipy>=1.7.0
imutils>=0.5.4
schedule>=1.1.0
Flask>=2.0.0
requests>=2.25.0

# Optional dependencies (install if needed):
# dlib - better tracking (heavy, optional)
#   pip install dlib
# ultralytics - YOLO detector
#   pip install ultralytics
# For model downloading:
#   pip install gdown
